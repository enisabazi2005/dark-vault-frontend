import "../Storage/Storage.css";

const Storage = () => { 
    return(
        <span className="storage-limit-warning">
        You have reached your storage limit. Switch to <span className="pro-text">PRO</span> to keep storing!
      </span>
    );
}

export default Storage;